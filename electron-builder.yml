appId: com.gardenmanager.app
productName: Garden Manager
directories:
  output: release
files:
  - dist-electron/**/*
  - dist-web/**/*
  - package.json
  - "!node_modules"
asar: true
win:
  target: nsis
  icon: build/icon.png
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: "${productName}-Setup-${version}.${ext}"
